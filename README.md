# How to build a local repo and and push it to github?  
`
git init  
touch README.md  
make changes to your file  
git add changed-file  
git commit -m "intial commit" 
git remote add origin remote-url
git push --set-upstream origin main
` 
